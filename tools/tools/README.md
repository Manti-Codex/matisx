# External Tool Manifests

MantisX core keeps only minimal built-ins. Extend runtime tools here.

- `manifest.json`: status/check manifest consumed by `/server_tools`
- Add executable checks under `tool_checks`
- Add file/script checks under `file_checks`
